services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    
    ports:
      - "8080:8080"
    
    volumes:
      # Código local
      - .:/app
      # Labs
      - /opt/containerlab/labs:/opt/containerlab/labs:ro

    # Corre 'go mod tidy'
    command: sh -c "go mod tidy && /go/bin/air"
